// Geliştirme ortamı için boş runtime config — Vite tarafından statik servis edilir.
// Üretimde container `docker-entrypoint.sh` tarafından üzerine yazılır.
window.__APP_CONFIG__ = {
  API_BASE_URL: ''
};
